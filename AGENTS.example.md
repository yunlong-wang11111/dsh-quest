# AGENTS.example.md —— dsh-quest 的多 agent 作业条令（通用版）

> 这是把生产环境里打磨出来的**行为纪律**泛化后的模板。dsh-quest 提供机制（spawn/回执/判定/收线），
> 这份文件提供"什么时候用哪个机制"的判断规则——没有它，AI 手里有枪但不知道什么时候开。
>
> **安装**：把本文件内容合并进你的 DSH user-global 指令文件（`$DSH_HOME/AGENTS.md`，DSH 会注入
> 所有会话），按需删改。占位符（如 `<你的解释器路径>`）替换成你的环境。QQ 群相关章节仅当你
> 配了通知桥时适用，否则用 README 里的 webhook 通道。

## 一、长任务一律走 quest 工具（最重要）

- **预计超过 1 分钟的命令**：必须用 `quest_run`（后台执行 + 自动判定 + 完成回执）。
  **禁止**直接跑长命令、用 Sleep 轮询 stdout、或挂后台 shell 等待。
- **多节点/有依赖/要复用的任务线**：`quest_plan` 写计划 → `quest_dispatch` 派发（after 链自动流转）。
- **盯进度**：派发时带 `progress_minutes: N`——服务端定时把日志尾摘要推回你收件箱。
  **Start-Sleep + 读日志是反模式**（锁回合/烧上下文/宿主重启白等）。
- 判断口诀：**"这条命令会不会超过一分钟？"** 会，就走 quest；不确定也走（成本更低）。

## 二、身份自查

- 不确定"我该向谁汇报、能不能直接找用户"时：调 `quest_status` 看返回的 `role` 字段。
  `main` = 本工作区主对话（汇总筛选后决定是否打扰用户）；`subagent` = 子对话（结果交回主对话）。
- 主对话换人：用户明确要求时 `quest_takeover`。

## 三、决策分级（别把该自己拿的决定上交给用户）

| 级别 | 例子 | 谁定 |
|---|---|---|
| ①机械分支 | "判定过没过→走 A 线/B 线" | 写进 plan 的 `when:` 自动路由；来不及写的自己定 |
| ②研究战术 | 补哪个对照/几 seed/先做哪个/怎么修脚本 | **主对话自己定，汇报里带一句** |
| ③主张/预算/方向 | 撤回结论、开新方向、大额算力 | 点名用户（quest_notify），**必须带建议** |

- 口诀：**"选错了能靠补实验纠正吗？"** 能=②自己定；不能/伤结论=③点名。
- 同一条流水线不许前几步自己跑、最后一步突然问用户。

## 四、小组制（lane 组）：什么时候派多个子对话并行

- **触发条件（满足才编组）**：≥2 条**独立的**备选路线，且每条产出**可独立验证**（对同一真值可互检）。单路线不编组。
- **成本纪律**：①共享地基单份（接口/IO/评测框架主对话写一次，lane 只写差异段）；
  ②探针并行、施工串行（先小探针选路再全量施工）；③纸面分析 lane 最便宜。
  **不可逆的决定才值得 3×，可逆的小步永远 1×。**
- **收口**：简报互检——一致=结论被独立验证；分歧=假设不稳的信号。对比结论派发者自己写。
- 用 `quest_spawn` 派出（handoff 写清判据与产物路径）、`quest_tell` 中途引导、简报自动回交。

## 五、冒烟、验收与通知

- **冒烟跟着代码走**：谁写的谁冒烟（主对话探自己的主线代码，子对话探自己的产出）；
  **判据在派发者，裁决在机器**（success: 判据 + 判定器），三者别互相代劳。
- **任务线收敛**没有自动通知（如你关了 converge）：做完一大块、或到了③级岔口，**你自己**发起汇报。
- 通知纪律：点名首行必须是"决策点/结论 + 建议"；更正/撤回类首行写"更正："可绕过冷却。
